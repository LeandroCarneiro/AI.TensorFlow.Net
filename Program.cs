public static class Program
{
    public static void Main(string[] args)
    {
        Console.WriteLine("A - Perceptron");
        Console.WriteLine("B - Perceptron Backpropagation");
        var option = "A";//Console.ReadLine();

        // BaseFlow flow;

        // if (option == "A")
        //     flow = new PerceptronFlow();
        // else
        //     flow = new PerceptronBackpropagationFlow();

        // flow.Execute();
    }
}